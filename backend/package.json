{
  "name": "learning-center-backend",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "test": "vitest run",
    "test:watch": "vitest",
    "deploy": "doppler run -- wrangler deploy",
    "secrets:sync": "doppler secrets -c prd --json | jq -c 'with_entries(select(.key | startswith(\"DOPPLER_\") | not) | .value = .value.computed)' | wrangler secret bulk",
    "db:migrate:local": "wrangler d1 migrations apply learning-center-db --local",
    "db:migrate": "doppler run -- wrangler d1 migrations apply learning-center-db --remote"
  },
  "dependencies": {
    "hono": "^4.12.3",
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.12.18",
    "@cloudflare/workers-types": "^4.20260305.0",
    "typescript": "^5.9.3",
    "vitest": "^3.2.4",
    "wrangler": "^4.69.0"
  }
}
